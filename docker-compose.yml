services:
  backend_nodejs:
    env_file: ".env"
    build: .
    restart: always
    environment:
      - NODE_ENV=production
    ports:
      - 3030:3030
    networks:
     - polo_net

    command: ["npm","run","start"]
